describe('Sample test', () => {
  beforeAll(() => {
    console.log('Test is running!');
  });

  it('should work', () => {
    console.log('Running test case');
    expect(true).toBe(true);
  });
});
